# Sen Live2D Android 任务总账

> **这是本项目唯一的任务与交接事实源。** 新对话、新 AI 或新开发者必须先完整阅读本文件，再检查仓库和构建记录。不要只根据聊天摘要继续修改。
>
> 维护规则：每轮动手前先写入“本轮制作、待实机确认”；用户确认后，再移入“实机已确认”。失败结果也必须记录，不能删除或反复覆盖打补丁。任何新发现都要同步更新“踩坑点”和“下一步顺序”。

## 核心重点：每次修改必须同步更新本总账

**任何源码、参数、界面、构建或实机结论发生变化时，都必须在同一轮更新本文件。不得只改代码、不记总账；不得把“编译成功”写成“实机确认”。每轮开始先登记计划，完成后补齐commit、Actions、APK校验和待实机清单。**

## 状态说明

- `已制作待确认`：代码和构建已完成，但用户尚未在目标手机/电脑确认效果。
- `实机已确认`：用户已经明确反馈结果。
- `已定位待修复`：原因已有直接证据，但修复包尚未实机确认。
- `暂缓`：有意推迟，不应被下一位 AI 擅自提前。
- `废弃路线`：已经证明不适用；禁止再次从头尝试。

## 当前目标

Sen 的长期静态错层已由C高精度蒙版解决，三套服装、原生物理、20个情绪入口和26个程序动作均已进入当前动态基线。本阶段仍不接DeepSeek或TTS；ZIP预设动作继续暂缓。

用户已在v0.4.7实机确认“呆毛原生直通完全正确”，证明`PhysicsSetting7 Hair_Z -> ParamAngleZ3 (Hair Z)`及moc3内作者根部绑定均正常，漂移只来自App旧的12点估算与后处理。Windows采集不再是必要前置。v0.4.8改为手机端在原生呆毛上直接采集发根与方向点的Drawable/三角形/重心权重，并只围绕该模型锚点调整形状。

## 当前版本与下一步顺序

- Android：`v0.4.8-ahoge-model-anchor-export-test`，**GitHub Actions已编译成功，APK结构与哈希已校验，等待目标手机采集/实机确认**。
- v0.4.7实机确认：开启原生直通后呆毛大小/位置恢复原版且根部固定、运动正确。由此确认模型、Cubism Core与原生物理链没有问题；v0.4.6的12点估算、根部位移/旋转/柔性三滑杆路线正式废弃，不再要求用户继续试数值。
- v0.4.8已制作并通过构建、待实机确认：采集按钮自动切到原生直通，允许用户先任意放大/拖动模型，再依次点击发根和朝尖端方向点。点击经当前MVP逆投影为Cubism模型局部坐标；实际持久化内容为Drawable ID、三角形三个顶点和三项重心权重，屏幕像素、舞台缩放和位移均不作为固定参数。
- 固定点完成后，App保留`model.update()`产生的原生物理顶点，只对全部长呆毛子网格施加同一个“以根点为不动点”的仿射变换；开放整体大小、长度、宽度、角度四项，X/Y平移固定为0。没有有效固定点时强制原生保护，禁止回退旧猜测路线。
- 舞台以青色/橙色标记实时显示根点和方向点；诊断区每0.5秒显示Hair Z、两个模型坐标、Drawable和四项形状值。一键导出JSON同时包含完整锚点、候选、当前参数、参考Drawable边界、仅供诊断的舞台变换，以及摸头模型局部`L/T/R/B`。
- 摸头坐标链已重新逐段核对：屏幕触点先依据当前投影边界逆换算为参考Drawable内`[0,1]`模型局部坐标，再保存/命中；人物放大、缩小、平移和屏幕宽高比不会改变已保存的摸头范围。当前确认值仍为`0.4927/0.0482/0.7095/0.1088`。
- Windows采集器v0.2.0保留为可选诊断工具，但由于Android原生直通已经证明与VTS原版一致，不再要求电脑采集后才能继续。
- 当前稳定基线保持：C高精度蒙版1024、三套内置服装、兔耳`135%/100%`双物理路线、完整26个程序动作按钮、持续柔风自主待机、尾巴右侧镜像和模型局部摸头范围。历史试错与各版构建证据保留在下方记录，不再混入当前执行队列。
- 下一步只做：手机安装v0.4.8、采集并导出JSON → 根据实机标记和外观固化锚点/形状 → 再进入原生嘴型与程序情绪试验台。

## 最近构建记录

### 2026-08-31 · v0.4.8 手机模型锚点、固定根部变换与联合导出（构建成功、待实机确认）

- 用户实机确认v0.4.7原生直通完全正确，电脑VTS采集从必要前置降为可选诊断。Android新增两点采集：触点先用与渲染一致的MVP逆投影得到Cubism模型坐标，再只在长呆毛子网格内命中三角形，持久化Drawable ID、三个顶点ID和三项重心权重。用户采集时可任意放大和移动模型，屏幕坐标不进入锚点。
- 根部和方向点优先选取同一Drawable候选；细线边缘未落入三角形时只允许在18屏幕像素等效距离内退到最近顶点，并在导出中明确标记。舞台每0.5秒投影青色根点/橙色方向点用于肉眼检查。
- 旧`12个顶点估计根部 -> 位移/旋转/局部柔性混合 -> X/Y偏移`处理不再执行。新调整直接从原生当前顶点出发，以根点为严格不动点，并以根→方向轴拆分长度/宽度，再应用整体大小和角度。四项初值为`56/100/83/-49`，X/Y固定为`0/0`；无完整锚点时回退原生保护而非猜点。
- 新增“导出呆毛+摸头诊断JSON”：包含锚点及候选、实时模型坐标、Hair Z、四项形状、参考Drawable边界、舞台缩放/位移（明确仅供诊断）和摸头`L/T/R/B`。摸头实现重新核对为模型参考边界归一化坐标，当前舞台缩放、平移和宽高比已在保存/命中前逆换算。
- 本地`git diff --check`与接口/构造调用静态核对通过。本地环境只有JRE、没有`javac`，且Gradle分发未缓存，因此最终编译由GitHub Actions验证。
- 远程功能 commit：`b4c34a153ca2125b236f1d37a27561c6217416ee`（与本地授权提交`b3c76e3`内容树一致）。首次 Actions run `33391643588` 因新导出逻辑漏引入`org.json.JSONException`而在Java编译阶段失败；失败日志已保留，其他Framework、资源与清单阶段均通过。
- 编译修复 commit：`ff0b17006417fff9c2d5db8ffab9db2d435e1d05`；Actions run `33392113594` 为`success`，Gradle `:app:assembleDebug`、APK生成和产物上传成功。
- Actions ZIP：`3,289,222` bytes，SHA-256 `c1c3c63248dd8604db30d7e15a097a88b6b6003888d163d2a9e8824d91c70a4b`，与GitHub artifact digest一致。APK：`3,801,031` bytes，SHA-256 `37abdc755bb279c447bda2ef87cff0ad15a0d555cca563d9b2eaba90946fa972`；ZIP与APK压缩结构均完整。

### 2026-08-31 · v0.4.7 原生呆毛直通、Hair Z诊断与VTS锚点采集（Android原生直通已实机确认）

- Android加入“呆毛原生直通”硬旁路：开启时App不写任何呆毛顶点，也不套用确认外观`56/83/-49/-16/+70`；这会改变呆毛外观位置，属于预期诊断现象，本轮只判断根部是否稳定。关闭后完整恢复v0.4.6三项后处理对照。界面实时显示`ParamAngleZ3 (Hair Z)`值与声明范围。
- Windows采集器升级到v0.2.0：连续记录根部点/方向点下所有重叠ArtMesh的三角形与重心权重；优先建议已知长呆毛候选，仍允许逐个轮换所有候选。临时青色标记通过`ItemPinRequest`钉在候选上，只清理本插件道具并在断开时自动卸载；保存包会记录最后一次成功放置标记的候选。
- 远程功能 commit：`99d318e161ddf23dd7dbfb56c787f3411fe56213`。Android Actions run `33382879333` 为`success`；Windows Actions run `33382879382`为`success`，两个工作流均一次构建通过。
- Android Actions ZIP：`3,281,829` bytes，SHA-256 `96a748c4bfd7d4dea0d1130526d2b0b8497d166261cce03d68c07470d603e90c`，与GitHub artifact digest一致。APK：`3,801,051` bytes，SHA-256 `2a28eadf5248f89c19b4ee5ddaec13d431d92c4af4d7b0213972ed5b7fb78d5c`。
- Windows Actions外层ZIP：`12,865,068` bytes，SHA-256 `be0788efd156284570109c88dcb255a1a014334832277e55bbd1c6d82347b136`，与GitHub artifact digest一致。内层分发ZIP：`12,861,082` bytes，SHA-256 `fe8b35ca701c3d093ffd214ac0e0c6324a7dfd3a4a0ab99a25fd84b49a02f4d7`；EXE：`13,112,247` bytes，SHA-256 `71054b1954137f795947e31de6972418c968cdf873154ca3bb473657e9d2dada`。两层ZIP结构均完整。
- 实机结果：用户确认原生直通呆毛“就是正确的”，根部固定、大小和位置均为模型原版。由此终止旧后处理调参和必须先用Windows采集的路线；进入v0.4.8手机模型锚点方案。

### 2026-08-31 · v0.4.6 呆毛三项运动幅度与模型局部摸头确认范围（构建成功；呆毛后处理路线已淘汰）

- 呆毛确认外观继续固定为`56/83/-49/-16/+70`；不再猜测或绑定外部头发、兔耳、尾巴Drawable。把呆毛自身原生运动拆成三项可实时调节并持久化的`0%～100%`滑杆：根部位移跟随、根部旋转跟随、局部柔性/抖动。默认试调值为`50/50/50`，三项`100%`等价于v0.4.5，三项`0%`为固定在模型坐标中的刚性对照。
- 用户在移动、放大后的模型上重新框选得到`L 0.4927 / T 0.0482 / R 0.7095 / B 0.1088`。由于框选值在保存前已经过当前投影逆换算，这组数值与舞台移动/缩放无关；已固化为新安装、覆盖升级和“还原确认范围”的版本化默认值。
- 完整26项程序动作按钮、兔耳确认值`135%/100%`、自主待机、三套服装和ZIP预设动作/表情没有在本轮删除或改写。
- 远程功能 commit：`2bde441926d96be079f1ea02072b4288001adda5`。首次 Actions run `33336067693` 因网页编辑器把`app/build.gradle`误写成`SenRenderer.java`内容，在Gradle配置阶段失败；该失败与Java功能逻辑无关，已如实保留记录。
- 构建配置修复 commit：`75346860a1f7b1ac14e295227b6049a72df264ae`；Actions run `33336465426` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传成功，耗时`2m 9s`。
- Actions ZIP：`3,280,348` bytes，SHA-256 `48ca5aadb30f17b483c19b3578afdba7fc1907c988b2292761620fdb68c20e15`，与GitHub产物Digest完全一致。APK：`3,784,651` bytes，SHA-256 `1c80dd1481bcb7b3e71d7bbe8ecfe867c4236a8de207dd9238046067a4c923fb`；ZIP与APK压缩结构均已完整校验。

### 2026-08-31 · v0.4.5 模型局部摸头与呆毛自身原生变形（构建成功、待实机确认）

- 摸头框选、数值显示和命中检测统一改为模型局部归一化坐标：触点经过当前模型平移、缩放、宽高比和投影矩阵的逆换算，因此范围会随人物移动和缩放，不再固定在屏幕上。保留双点框选与还原按钮；旧确认值`0.3875/0.2813/0.6234/0.3778`只作为坐标制迁移初值，等待用户重新框选后固化新值。
- 对照用户提供的原始VTS配置确认模型原生物理已包含`Head_X/Y/Z`、`Hair_Z`、`BangX/Y`、`Side Hair`、`Hair_behindEar`及实时`ParamHairS1/S2`输出。呆毛不再绑定或猜测任何外部头发、兔耳或尾巴Drawable；每帧在`model.update()`之后，以呆毛自身固定根部顶点跟随原生变形，再施加确认外观`56/83/-49/-16/+70`，不复制外部缩放。
- v0.4.4恢复的完整26个程序动作按钮继续保留；兔耳`135%/100%`、自主待机与ZIP预设动作/表情没有在本轮改写。
- 远程功能 commit：`c45450926509fcf7e557683d48bb54cd1b4a2157`；Actions run `33333443357` 为 `success`，Gradle `:app:assembleDebug`及产物上传一次成功，耗时`1m 36s`。
- Actions ZIP：`3,276,800` bytes，SHA-256 `7c5f126a8363faeff5eb27b9be6417736cac6c3ad19dc5e18a4ce203ab750a79`，与GitHub产物Digest完全一致。APK：`3,784,659` bytes，SHA-256 `099bdbe46fd1dbe8759eed9b19b20b1761440f3fce8c84de690c699e67ab3e40`；ZIP与APK压缩结构均已完整校验。

### 2026-08-31 · v0.4.4 完整动作入口与CDI头部支撑（构建成功、待实机确认）

- 对照v0.4.1恢复完整26个程序动作按钮，不再因自主待机职责自动隐藏；动作源码、待机池和ZIP预设动作/表情均未删除或改写。
- 呆毛不再从全模型1742个Drawable按距离猜测支撑。运行时读取模型`cdi3.json`，只允许名称明确属于头部、脸部、头发、刘海的Part直接子网格，并语义排除耳朵、尾巴、呆毛和头饰；CDI识别失败时不回退全模型猜测。
- 呆毛整体支撑只保留位移与旋转，强制归一化缩放，避免极限触屏跟随时忽大忽小；独立局部柔性由18%降至8%。确认外观`56/83/-49/-16/+70`不改。
- 远程功能 commit：`aeda14ab5d926f389c729cdb4f560a1a7936e19b`；Actions run `33329327041` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传一次成功，耗时`1m 21s`。
- Actions产物`Sen-Live2D-Renderer-Test-APK`页面显示`3.13 MB`，Digest为`sha256:2a382401d0d56d91c60e4381584f7df9445c8e5f8a1f6bea33d870f821dc95f6`。本轮浏览器普通下载受URL安全策略阻止，官方文件下载接口虽返回成功但未同步到共享目录，因此尚未取得ZIP/APK本地字节，不能填写精确APK大小或APK SHA-256；不得把Actions Digest误写成APK哈希。

### 2026-08-31 · v0.4.3 待机路由、呆毛排除兔耳与摸头确认范围（构建成功、待实机确认）

- 兔耳速度/幅度确认预设固化为`135%/100%`并保留独立滑杆；自主待机首次约`18～38秒`、后续约`28～68秒`低频触发九轴双抖，仍只混回三个兔耳物理输出。
- 自然眨眼按Sen实机观感由`0.22秒`调整为`0.34秒`；实际原版`wind_sway_soft`使用两段半周期错开的连续底座替换静止站立，常规池使用迷梦`4.8～11.5秒`间隔和最近3次防重复，展示池继续使用首次`28～60秒`、后续`48～100秒`及`55%/31%/14%`比例。
- 呆毛支撑选择会主动探测并排除所有受三个兔耳角度参数驱动的Drawable；恢复13个未加入自主待机的程序动作按钮，已承担待机职责的路线按钮继续隐藏，全部26条源码路线与ZIP预设不删除。
- 摸头校正按钮保留；确认范围版本化固化为`L 0.3875 / T 0.2813 / R 0.6234 / B 0.3778`，`还原确认范围`按钮准确恢复该组数值。
- 远程功能 commit：`2b0e63fe17fc24a00628b721d515fc8bcd97b6b0`；Actions run `33327587206` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传一次成功。
- APK：`3,784,639` bytes，SHA-256 `3a5b8f5dd77b8686bc3c0df78e9d455ba059dabe43f9af459de444976d7c200c`。Actions ZIP：`3,277,226` bytes，SHA-256 `7312af42c621c6f3044568b7afe9aa2db4f9cdb745fbedd8825b289457b70618`，与Actions产物Digest完全一致。
- ZIP压缩结构完整；APK内已确认包含`v0.4.3`、兔耳`135%`显示和摸头确认范围。自然眨眼、静止替换、低频兔耳双抖、呆毛排除结果、按钮范围和展示池仍须目标手机实机确认。

### 2026-08-31 · v0.4.2 兔耳调速、呆毛头发绑定与自主待机（构建成功、待实机确认）

- 保留v0.4.1已经实机确认的九轴→隐藏物理→仅复制三个兔耳输出路线，新增互相独立的兔耳速度/幅度`50%～250%`调节；默认速度`150%`、幅度`100%`。
- 呆毛外观固定为`56/83/-49/-16/+70`，删除外观滑杆和点选发根；自动从周围头发/头部网格取得多点支撑，以整体位移、旋转、缩放带动呆毛，局部独立柔性降至`18%`。尾巴固定在右侧。
- 摸头范围改为双点框选，最小宽度`8%`、最小高度`6%`，界面保存并显示`L/T/R/B`归一化坐标，等待用户回填后固化。
- 自主待机改为持续柔风底座，常规池加入环顾、待机歪头、叹气下沉等动作；低频展示池按迷梦时序和随机比例加入明显风摆、视频式环绕、展示级大摆并防连续重复。26个程序动作按钮已从界面移除，源码路线和职责注释保留；ZIP预设动作/表情未修改。
- 远程功能 commit：`347c158ce298ce77d8cffe4bd0aa59a1366725e7`；Actions run `33323410809` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传一次成功。
- APK：`3,784,651` bytes，SHA-256 `0f3835e44f8ef36c644f43e6b973a98bcae53e605744b30fb75fc6c069d3eb2d`。Actions ZIP：`3,275,373` bytes，SHA-256 `7acd6a994cc675c1c3cd9285983beb4f4f7117bd6935764f4434e5df31b47831`。
- Actions ZIP与APK压缩结构均已完整校验，APK内也确认包含v0.4.2版本、兔耳调节和摸头坐标字段；兔耳最终百分比、呆毛大动作贴合、摸头范围和自主待机随机体验仍须目标手机实机确认。

### 2026-08-31 · v0.4.1 九轴兔耳、连续动作与待机眨眼（构建成功、部分实机确认）

- 兔耳恢复九轴→原生物理的柔性路线，通过双物理实例隔离，正常全身物理不受耳抖驱动污染；仅三个兔耳输出取自隐藏物理。
- 26个程序动作全部经过运行时完整性校验：动作名与曲线一一对应，普通关键帧必须从0进入并回到0；中间插值改为连续速度曲线，动作中途切换增加交叉过渡。
- 20个程序情绪继续使用`0.42秒`交叉淡化；ZIP原生预设动作/表情按用户要求不改。
- 自主待机加入独立自然眨眼调度，并修复首次进入App按钮文字为空。
- 呆毛确认默认值升级并清除旧错误根部记录；点选保留五项外观值、显示可回填数值，局部柔性摆幅暂收至`60%`。
- **实机结论**：用户确认兔耳九轴路线与眼睛隔离已经正确，继续保留；速度仍不够像猫耳快速双抖，进入v0.4.2可调。呆毛在大幅点头/摇头时仍漂浮且幅度过大，点选根部路线废弃。
- 远程源码 commit：`f775b906addc0021aa496ac8b8fbadd2e68b3d33`；Actions run `33320370257` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传一次成功。
- APK：`3,784,651` bytes，SHA-256 `0af05f3e079c39d167c4f02a86c79396b88e8e928569be0e791e9ac387f82668`。Actions ZIP：`3,276,679` bytes，SHA-256 `4dcaaf41ea1216a7ccb5078312969a9ee3d37b3cacc67bdc88f7f7c3b6db170d`。
- 构建产物ZIP与APK压缩结构均已完整校验；九轴耳抖幅度、眼睛隔离、自然眨眼、所有程序动作/情绪连续性、点选根部读数和呆毛柔性仍须目标手机实机确认。

### 2026-08-31 · v0.4.0 互动与平滑回正（构建成功、待实机确认）

- 保留v0.3.9耳朵双脉冲时序/曲线，只隔离并放大兔耳角度输出；眼睛、低头、呼吸和其他九轴不参与耳抖。
- 移植迷梦极限触屏跟随、头部区域连续抚摸、常规摸头和疑惑大小眼彩蛋，但按Sen参数/物理方向重新映射。
- 统一处理动作结束、手臂物理释放、语义情绪切换和ZIP表情退出的平滑回正。
- 增加实机点击呆毛根部并绑定动态顶点的校准路径；不写回购买模型、`.moc3`或贴图。
- 三个服装预设共同补入`ArtMesh1324`与`ArtMesh1689 = #444573`；女仆装/白衬衫共用表和兔女郎独立分支都从公共覆盖表取得这两项。
- 远程源码 commit：`b155f2298756aa5891a3e5c5418659877eb65b58`；Actions run `33317647358` 为 `success`，Gradle `:app:assembleDebug`、APK生成和产物上传一次成功。
- APK：`3,784,639` bytes，SHA-256 `9326eb1de121a650adb9fab5619c03cf13636d7eccf92d9a737dc71f98e7ee0e`。Actions ZIP：`3,270,870` bytes，SHA-256 `ada65226de4893d6bf6ac1c86b57bd1c2404c2a45a15fd80e504513f05705e36`。
- 构建与压缩结构完整性已经确认；兔耳幅度、触屏跟随、摸头、大小眼彩蛋、动作/表情回正和呆毛根部绑定仍须目标手机实机确认，不能提前移入“实机已确认”。
- **实机需调整**：兔耳路线与用户原意相反；动作结尾瞬移未完全消失；摸头/疑惑和视频式环绕在中间关键点可见停顿；点选发根会改变呆毛朝向且没有可回填数值；自主待机缺少独立自然眨眼，首次按钮文字为空。以上进入v0.4.1修正。

### 2026-08-31 · v0.3.9 动作路线与局部物理优化（构建成功、待实机确认）

- 固化用户截图确认的呆毛五项参数，同时保留全部精调滑杆。
- 耳朵快速双抖改为复用慢眨眼已经验证可带动耳朵的九轴→原生物理链路，压缩为两次短脉冲；不再直接覆盖耳朵物理输出。
- 24个动作迁移迷梦关键帧和风摆采样路线；所有动作保持头身幅度增强，但动作期间仅对Sen双侧肩膀、上臂、前臂、手部物理输出施加局部衰减。
- 远程源码 commit：`a838b704be1516ae60691be062aca89ab8f190e9`；Actions run `33312587853` 为 `success`，Java编译、APK生成、测试签名和产物上传一次成功。
- APK：`3,768,251` bytes，SHA-256 `5dc0f43a44950aea5cb6641e4ec0e4845724df5f33cf5fb490157e9ed9e84764`。Actions ZIP：`3,264,006` bytes，SHA-256 `72ee2e9a76d3feafee3f98122a317f9b5ccf0f8284139c02c9bef530a54ebedb`。

### 2026-08-31 · v0.3.8 三套内置服装预设（构建成功、待实机确认）

- 新增独立`SenOutfitPresets`源码类，内置女仆装、白衬衫、兔女郎三套服装；每套只含16项服装白名单参数，明确排除581项采集中的九轴、眼睛、手臂、呼吸、表情和物理瞬时值。
- 女仆装/白衬衫使用同一配色表，兔女郎使用独立配色表；切换前会停用全部旧Drawable颜色覆盖，再应用83项完整目标颜色，防止兔女郎灰色残留到另外两套。
- `ArtMesh210`与`ArtMesh276`已按用户确认补为`444573FF|000000FF`；白衬衫固定`Hair_behindEar10=0`，女仆装与兔女郎按采集值保持1。
- 服装按钮在当前已加载模型内即时切换，不重新创建139 MiB模型。App仍需保留一份已导入的VTS动态外观底座，但以后切换服装不再选择任何服装JSON；完整采集文件和购买模型均不进入公开仓库。
- 耳鳍抖动不再错误借用`ParamBreath`，改为原生物理后只叠加兔耳专用角度输出的两次短脉冲。呆毛纵向轴改称“高度”，继续以发根为固定锚点。
- 远程源码 commit：`39fd119e19aaf76930e94e36358491247c1f3316`；Actions run `33311272398` 为 `success`，Java编译、APK生成、测试签名和产物上传均成功。
- APK：`3,768,251` bytes，SHA-256 `f8e7197c3b14e8e54c4dfe0cfb73ca30881b798290d37db77dd77ad2ec337ae1`。Actions ZIP：`3,256,927` bytes，SHA-256 `0474cc18a64d01fb02b239f7ce1829ce064e7825bb9ce9373e87de18904c472a`。

### 2026-08-31 · v0.3.6 精确部件调整（本轮制作、待实机确认）

- C高精度蒙版正式保留`512px`与`1024px`两档，后续分别测试帧率、发热、加载速度与耳缘白边；`256px`不再作为C画质选项，A/B仍仅保留底层诊断用途。
- v0.3.5实机确认耳鳍角度有效，但`Part56`子层级上下位移在`-50`时仍完全无变化，说明屏幕上当前耳鳍并非该命名Part的有效绘制层。本轮禁止继续猜Part名称。
- 本轮在模型加载时临时对三个已确认有效的耳鳍角度参数写入两组对照值并各更新一次模型，逐Drawable比较顶点变化，自动记录真正受耳鳍参数影响的网格；随即恢复用户当前角度，再把上下位置应用到自动识别结果。对照只发生在渲染器就绪前，不会形成可见闪烁。
- v0.3.5实机确认长呆毛Part层级定位成功，“上移”和“缩短”均有效。本轮将两个粗略开关替换为精细数值：大小、旋转角度、左右位置、上下位置；缩放和旋转以呆毛发根为锚点，界面持续显示最终数值，供用户确认后固化默认值。
- 尾巴总父部件已由CDI确认是`Part239（尾巴）`。本轮增加左侧/右侧切换，只镜像该Part下当前可见子Drawable的X坐标，镜像轴固定为模型局部中心线`X=0`；人物、脸、身体与模型舞台位置不得移动。
- 所有部件调整仍只保存在App偏好并在运行时叠加，不写回购买ZIP、`.moc3`、贴图、`.vtube.json`或VTS采集文件。实机确认前全部标记为待确认。
- 远程源码 commit：`1c4717149b1133dc588936bff7aa7355795b7af5`；Actions run `33302498081` 为`success`，Java编译、APK生成、测试签名和产物上传均成功。
- APK：`3,751,883` bytes，SHA-256 `eb3e145b1e070e6107d09e44e4557a1252c9a6ad9e0f92d7e455e11d9df51d8e`。Actions ZIP：`3,246,735` bytes，SHA-256 `76a823178a8d7b5c0d2dc0ed04bb9ccd60efe49cd8314eba4493cee4ed088b78`。

### 2026-08-30 · v0.3.5 耳鳍位置与呆毛层级修复（实机部分确认）

- v0.3.4实机确认耳鳍角度参数有效，且负数才是向外放平；旧滑杆方向说明与快捷值不符合实际需求。本轮改为`-50°～+30°`，提供原VTS值、`-20°`、`-35°`、`-50°`快捷值。
- 模型声明的耳鳍角度下限为`-30°`。`-30°～-50°`通过与VTS冻结值相同的Core ParameterView越界直写进行实验；必须实机比较`-30°`与`-50°`，若画面相同，说明模型关键形状在`-30°`截止，后续才增加独立几何补偿，不能只让UI显示虚假数值。
- 增加“耳鳍上下位置”滑杆，范围`-50～+20`、`0`为原高度、负数向下；数值按模型画布千分比映射。通过`Part56（鱼鳍耳）`递归取得子Drawable，使外侧、内侧、阴影等同一部件一起移动，不逐个猜测ArtMesh。
- v0.3.4的“缩短/上移”实机完全无效。根因已确认：CDI中`ArtMesh140_Skinning2`和`ArtMesh140_Skinning`虽然名字含ArtMesh，实际登记在`Parts`列表，旧代码却使用`getDrawableIndex()`查找，结果目标数为0。
- 本轮改从`Part13（呆毛）`、`Part220（呆毛长旋转）`及两个蒙皮Part递归取得全部子Drawable，仅变换当前可见且非零透明度的网格；渲染诊断新增“呆毛子网格总数/可见数”和“耳鳍子网格总数/可见数”。
- 新`.vtube.json`导入会覆盖App私有目录中的旧逐ArtMesh颜色文件，不与旧颜色叠加；同一模型只改颜色时重新导入新`.vtube.json`即可。若同时切换服装、耳鳍、丝袜、发型等参数状态，还必须同步导入新采集的`Sen.vts-profile.json`。
- 本轮不修改购买ZIP、`.moc3`、贴图或VTS文件；角度、位置和呆毛变形均为运行时可撤销测试。构建成功不代表真机几何已经确认。
- 远程源码 commit：`1dc45914b4bb31cb3e43f75f9065f83022822be9`；Actions run `33301312953` 为`success`，Java编译、APK生成和产物上传均成功。
- APK：`3,751,887` bytes，SHA-256 `65a7959e580f902c1c6b447a5290468dcf4d5a06810c3da152e0480bdfc7bd4e`。Actions ZIP：`3,243,424` bytes，SHA-256 `5806c29a909a4b712cb98bdd7e9fefe8eee06328d20156db0772f9c8962475ab`。
- **实机结论**：512px与1024px都能正确显示，继续保留等待性能对比。长呆毛已经能上移和缩小，说明Part层级修复有效，可进入旋转、X/Y和连续大小精调。耳鳍高度即使设为`-50`仍没有任何移动，`Part56`目标判断失败，必须改为参数影响网格自动识别。用户要求当前左侧鲸尾围绕人物中心线镜像到右侧。

### 2026-08-30 · v0.3.4 C蒙版清晰度、耳鳍与呆毛试调（实机部分确认）

- C高精度蒙版增加256/512/1024三档，默认1024；A/B只保留为底层诊断，不作为正式显示方案。重点确认耳鳍外缘的细锯齿/白边是否随蒙版分辨率提高而减轻。
- 耳鳍只联动测试模型已有的`ParamL_angle`、`ParamR_angle`和`ParamR_angle2`，保留原VTS值，并提供原值、0°、+10°、+20°及完整-30°至+30°滑杆；不修改网格和购买文件。
- `ParamHair_Hue4`已经确认是呆毛类型选择，不得当作长度参数。本轮改为仅对CDI明确标出的`ArtMesh140_Skinning2`和`ArtMesh140_Skinning`两个“呆毛长（蒙皮）”网格做运行时测试变换。
- 呆毛提供两个互相独立且默认关闭的选项：“缩短”和“上移”。缩短以网格下缘（发根侧）为锚点，上移整体平移；任何选项均不写回ZIP、`.moc3`、`.vtube.json`或VTS采集参数。
- 耳鳍和呆毛试调值必须能即时恢复并在蒙版切换/模型重载后保持，方便后续接入动态时检查是否与物理或九轴冲突。若目标ArtMesh不存在，只显示诊断，不得误改其他头发。
- 本轮仍不启用物理、表情、九轴或AI；先验证静态画质和试调几何是否安全。所有效果在用户实机明确反馈以前都只能写为“待确认”。
- 远程源码 commit：`ca7db2c991a988053b0787e64e8409c39fc37f3f`；Actions run `33298690990` 为`success`，Java编译、APK生成和产物上传均成功。
- APK：`3,751,887` bytes，SHA-256 `b61b17f8cd218acb6810aa2e3f4fb44fad8377e6dafd8bc0554bdbaf26de0561`。Actions ZIP：`3,241,330` bytes，SHA-256 `1391ac373ca672ed7cca7dc7dfc1f94663cb7e30e5019b22a16b42f85dd2f799`。
- **实机结论**：耳鳍参数调节有效，但方向与旧UI预期相反，负数才是放平；`-30°`时耳鳍根部仍从头发上方伸出，需要单独向下平移。呆毛“缩短/上移”完全无变化，随后确认是把Part误当Drawable查找。1024蒙版边缘效果尚未单独确认，继续保留等待测试。

### 2026-08-30 · v0.3.3 Cubism官方蒙版A/B/C诊断（本轮制作、待实机确认）

- 本轮只验证底层静态渲染，不改581项VTS冻结值、79项ArtMesh颜色、外观、表情或动作。
- 增加三种均基于官方Cubism Java R5的渲染模式：A为当前默认单缓冲基线，B按模型实际唯一蒙版组数动态分配多缓冲，C强制官方高精度蒙版逐组渲染。
- App必须显示Drawable蒙版组数、Offscreen蒙版组数、实际缓冲数、Blend/Offscreen能力和高精度蒙版最终状态；若B或C能修复白块、缺刘海、嘴、手指、丝袜和耳朵错层，即可直接定位为蒙版容量/生成路径问题。
- 同时修复预览调整：双指缩放围绕两指焦点计算，单指接管时重新取得有效指针，限制异常平移，避免放大后模型偶发移出画面。
- Inochi Creator使用Inochi2D自己的模型和渲染体系，不能读取Cubism `.moc3`，仅能作为“蒙版需要明确隔离与合成”的概念佐证；本项目不引入其代码、不增加所谓C渲染器，避免污染Cubism诊断路线。
- 首次编译 run `33297316523` 失败：`CubismRendererAndroid.create()` 的公开返回类型是父类 `CubismRenderer`，接收为Android子类时必须显式转换；仅此一处编译错误，已修正后重建。此失败不代表A/B/C算法或模型实机结果。
- 修正后源码 commit：`cff7c15dce17fe94c8af14df0b537f0d53d9bd90`；Actions run `33297412426` 为 `success`，Java编译、APK生成、签名和产物上传全部成功。
- APK：`3,751,867` bytes，SHA-256 `31a64fa30e37e9bd554c44c2149f8ec07f220cfaf2b6a00702ceb4541578d42e`。
- Actions ZIP：`3,236,013` bytes，SHA-256 `c4336fdfcf398aaf76504830b25db77dc6e7a9422e19e2c564524a0031b29478`。
- 当前仍为“已制作待确认”。Actions成功只证明代码和官方Cubism接口正确编译，不代表三种蒙版模式的实机画面已经正确。
- **实机结论：长期静态显示问题的主因已经确认是蒙版渲染容量/方式。** Sen实际有60个唯一Drawable蒙版组、354个被蒙版对象、无Offscreen、无Blend；A的单缓冲容量只有36组，必然溢出并造成白块、缺刘海、嘴、手指和耳朵错层。
- B动态多缓冲使用D2/O0后，全部部件恢复，但一个256×256蒙版缓冲仍要分区容纳多组蒙版，出现非常明显的阶梯锯齿，嘴等细小部件会被锯齿切割，不能作为正式方案。
- C强制高精度蒙版后全部部件正常，说明逐蒙版重绘路径正确；耳朵外缘仍有很细的锯齿/白边。当前高精度蒙版缓冲仍是官方默认256×256，下一步只提高C的Drawable蒙版缓冲分辨率，不再调整部件参数。

### 2026-08-29 · v0.3.2 VTS静态冻结对照（本轮制作）

- 远程源码 commit：`f6ad4f6486ff47f8cba9bf3c0710626a91987d81`。
- Android Actions run：`33276813221`，结论 `success`；Java编译、APK生成和产物上传全部成功。
- APK：`3,751,867` bytes，SHA-256 `65e0f5ba103471d956a46438d53b8ad05b8b1d0954479852f94c5ce7a3d13a3e`。
- Actions ZIP：`3,232,877` bytes，SHA-256 `ba9a5b288aa3bf12aed304bde89700b8bbe0dbf276fdcacf1a845a93c848fbfc`。
- 本轮唯一目的：判断“相同VTS最终参数下仍显示错误”还是“此前从未真正复现完整VTS最终状态”。不移植动作、表情切换、物理、九轴或AI功能。
- 导入 `Sen.vts-profile.json` 的全部581项当前值，不再排除203项动态值；冻结模式停用表达式管理器、物理更新和每帧参数恢复，只在初始化时计算一次Drawable。
- 参数通过Core ParameterView直接写入，故意绕过Framework的范围裁剪。直接证据：VTS采集的 `Warning2=-1`，但模型声明最小值为0；v0.3.1把 `VTS_Add`改成普通`Add`会丢失这个状态。
- 同时应用当前 `.vtube.json` 的79项ArtMesh乘算/屏幕色；保留“旧分层状态”和“原始状态”用于A/B对照。
- 上方预览支持“调整模型”开关、单指拖动、双指缩放和一键还原；此功能只方便检查，不参与模型参数计算。
- 若冻结模式仍出现同一批几何/透明度错误，才把调查重心转向Cubism Java与VTS对Cubism 5高级功能的实现差异，并启用局部Drawable定位工具。
- **实机已确认失败**：581项冻结参数与79项ArtMesh颜色均已加载，但右上白块、缺刘海、嘴部小点、手指/指甲分离、丝袜透明度和耳朵错层均无改善。由此排除“普通参数未完整导入”作为主因。
- “调整模型”存在独立缺陷：双指缩放后模型可能被推到画面外，点击还原恢复；该问题属于预览矩阵/手势坐标，不是模型Drawable消失。

### 2026-08-29 · v0.3.1 VTS分层外观

- 用户补充了实际 `xiaojingyu.exp3.json` 与当前 `Sen Customizable Model_2K.vtube.json`，两者只用于离线分析与 App 私有导入，不提交公开仓库。
- `xiaojingyu` 只有55项修改：54项 `Add`、1项VTS专用 `VTS_Add`；它是相对原模型默认值的增量，不能单独当完整模型状态，也不能与API最终快照混用。
- 当前 `.vtube.json` 另有79项 `ArtMeshMultiplyAndScreenColors`，直接保存紫色头发、服装、尾巴、鞋子等逐Drawable乘算/屏幕色；颜色并不全是Live2D参数。
- v0.3.1 停用旧581项快照，改为三层顺序：原模型默认值 → `xiaojingyu`增量 → 79项VTS ArtMesh颜色；支持分别或同时导入两个JSON。
- 远程源码 commit：`1eb9ea0338539ef5444dd3a884f413fc8cd014cc`。
- Android Actions run：`33273797043`，结论 `success`；Java编译、APK生成和产物上传全部成功。
- APK：`3,735,487` bytes，SHA-256 `0097a46bf2f395b153a2a9393491fdd576487a6435aa8322e20283885002025d`。
- Actions ZIP：`3,226,598` bytes，SHA-256 `b33063e92cf7d46805c03935f0a7418c8d824105c0dfda9e921a19cead1ee926`。
- 构建成功只确认代码和官方Cubism接口可编译；三层外观是否与VTS一致仍必须由目标手机实机确认。
- **实机已确认失败**：颜色层大体生效，但长期存在的右上白块、缺刘海和手指/指甲浮空未解决；白色丝袜接近透明，嘴部静止时只剩小点。点击ZIP预设还会替换掉 `xiaojingyu` 外观层并整体回退。
- v0.3.1的两个结论已撤销：一是 `VTS_Add`不能无证据转换为普通`Add`；二是“原始默认值→xiaojingyu→颜色”不足以复现VTS静态状态。

### 2026-08-29 · v0.3.0 VTS静态参数导入

- 远程源码 commit：`c2c70a693bec2d5c10bb1b69e16bba57ff6f947f`。
- Android Actions run：`33272637867`，结论 `success`；产物 `Sen-Live2D-Renderer-Test-APK`。
- APK：`3,735,483` bytes，SHA-256 `182245a28b23958472ab15ab53fa457813e505603a545f78ce46f67be9106a44`。
- 用户提供的 `Sen.vts-profile.json` 仅用于离线校验，未提交仓库：581个参数全部能匹配 Sen 模型；应用378个静态参数，排除203个 VTS跟踪/物理输出，模型缺失参数0个。
- 采集器已由用户成功连接VTube Studio并导出参数包；参数包检测到活动的 `Watermark.exp3.json` 与 `xiaojingyu.exp3.json`，说明连接、授权、采集和保存主流程已走通。
- 构建成功只确认代码可编译，颜色、头发、眼睛和部件状态仍必须由目标手机实机确认。

### 2026-08-29 · v0.2.1 / VTS采集器v0.1.0

- 远程源码 commit：`446b3feb74b6e4e7f637fae5ca1bb9ec3e65ea06`。
- Android Actions run：`33255401361`，结论 `success`；Framework低显存贴图补丁与APK构建步骤均成功。
- 总账/工作流收尾 commit：`b479362b31e73cc157ec765c77e47a4fca052371`；更新路径过滤后的复核 run `33255534635` 同样为 `success`，功能源码与交付APK内容不变。
- Windows Actions run：`33255401296`，结论 `success`；EXE构建与带说明文件的ZIP封装步骤均成功。
- `Sen-Live2D-v0.2.1-native-texture-fix.apk`：3,735,483 bytes，SHA-256 `89fe4d804a37668a41092d70cf7a6f33600974ce87ed8fd68c1a78705bfcabc5`。
- `Sen-VTS-Parameter-Capture-Windows.zip`：12,851,236 bytes，SHA-256 `dd0e7b1306a320a0ad563d41f408b524c2ea83f08293f01c5e83b7cfcf0d4b2c`；内含EXE、使用说明和第三方声明。
- 两个产物均为“已制作待确认”，不能因 Actions 成功而写成“实机已确认”。
- 从本轮起 APK 工作流限制为源码、Core、Framework、补丁和构建文件变更时触发；单独更新总账/README不再浪费APK构建。

## 已确认的模型事实

- 用户提供的是 `Sen Customizable Model_2K`，模型不能提交到公开仓库或打入 APK。
- `.moc3` 使用 Cubism 5；旧项目的 `pixi-live2d-display/cubism4` 不是合适的渲染底层。
- ZIP 中有 26 张 2048 贴图。原始 PNG 合计约 18 MiB，但上传 WebGL 后约占 `26 × 2048 × 2048 × 4 ≈ 416 MiB`，还不含 Cubism 蒙版和缓冲区。第一阶段先验证原始2K；实机出现“无错误但全黑”后，新增不覆盖原文件的1K兼容副本用于判断显存问题。
- 原始 `model3.json` 没有登记 `FileReferences.Expressions`，但 ZIP 根目录中实际存在多个 `.exp3.json`。导入器必须扫描并补登记，官方 Framework 才会加载这些表情。
- 当前 `.vtube.json` 的 `SavedActiveExpressions` 同时包含 `Watermark.exp3.json` 与 `xiaojingyu.exp3.json`。前者和后者都涉及 `Warning` 至 `Warning6`，其中 `Warning2` 在 `xiaojingyu` 中使用VTS专用 `VTS_Add`；不能再用单表达式管理器或普通`Add`近似。
- VTube Studio 的参数映射、输入输出范围、平滑、物理强度等保存在 `.vtube.json` 中；这些将在静态显示通过后再移植。

## 技术方案

- v0.1.x 使用 Android 原生壳 + `WebViewAssetLoader`，WebView 内使用 Live2D 官方 Cubism Web Framework 5.3 兼容版本，不使用 Pixi Cubism 4 插件。
- **实机已经证明 Web 方案不适合 Sen 的超大型 `.moc3`。v0.2.0 已改用官方 Cubism SDK for Java 5 R5（Android 原生 OpenGL），不再继续围绕 WebView 黑屏打补丁。** 用户已从 Live2D 官方取得并提供完整 `CubismSdkForJava-5-r.5.zip`；Core AAR 校验完整。
- `Framework` submodule 固定到官方 `CubismJavaFramework` tag `5-r.5` / commit `c2d4200...`；APK 收录官方许可清单允许再分发的 `Live2DCubismCore.aar`，不再需要联网或用户另行导入 Core。
- 原生加载关闭 MOC consistency 重复检查；贴图逐张解码上传并立即回收 Bitmap，不生成 mipmap。App 开启 `largeHeap`，为139.2 MiB moc3的首次 Java byte[] 与 Core Model 创建留出空间。
- 模型 ZIP 只解压到 App 私有目录；仓库和 APK 不含模型。

## 待实机确认

### v0.4.8-ahoge-model-anchor-export-test（构建成功、待实机确认）

- [ ] 模型放大、移动后点击发根与方向点，诊断显示Cubism模型坐标、Drawable和重心锚点；还原/再次改变舞台大小与位置后，青色根点仍贴住同一发根。
- [ ] 关闭原生直通后，整体大小、长度、宽度和角度四项可实时调节，根点不发生位移，原生Hair Z摆动继续存在；X/Y始终显示并导出为0。
- [ ] 退出重进或覆盖安装后，已采集的Drawable/三角形/权重可恢复；模型结构不匹配时自动回退原生保护，不使用旧12点猜测。
- [ ] “导出呆毛+摸头诊断JSON”可正常选位置保存；文件同时包含锚点、候选、形状、Hair Z、参考边界、舞台诊断快照和摸头范围。
- [ ] 在不同舞台缩放/平移下重新框选同一摸头位置，导出的`headPatRange`仍属于模型局部归一化坐标；当前确认范围`0.4927/0.0482/0.7095/0.1088`随模型移动缩放。
- [ ] 完整26项动作、兔耳`135%/100%`、三套服装、自主待机和尾巴右侧镜像无回归。

### v0.4.6-ahoge-motion-headpat-preset-test（历史失败路线；不再继续调参）

- [ ] 三项呆毛运动滑杆均可`0%～100%`实时调节并覆盖安装后保存：根部位移跟随、根部旋转跟随、局部柔性/抖动；界面和诊断摘要显示精确整数值。
- [ ] 三项全`100%`时与v0.4.5运动等价；全`0%`时呆毛保持确认外观并只随整个模型舞台移动/缩放，不再响应头部位移、头部旋转或自身物理抖动。
- [ ] 从默认`50/50/50`开始，分别压低单项并测试极限触屏跟随、点头、摇头、柔风；将最自然且不乱跑的三项数值回填，下一版再固化最终运动预设。
- [ ] “还原确认的呆毛预设”只恢复外观`56/83/-49/-16/+70`，不覆盖当前运动试调值；“还原呆毛运动试调初值”只恢复`50/50/50`。
- [ ] 摸头还原值为新模型局部确认范围`L 0.4927 / T 0.0482 / R 0.7095 / B 0.1088`，人物移动和缩放后仍跟随头部。
- [ ] 完整26项程序动作按钮、兔耳`135%/100%`、自主待机、三套服装与ZIP预设均无回归。

### v0.4.5-model-space-headpat-native-ahoge-test（本轮制作、待实机确认）

- [ ] 在任意模型位置/大小下框选摸头区域，界面显示新的“模型局部归一化”`L/T/R/B`；随后拖动、放大或缩小模型，实际可触发区域始终跟随人物头部，不固定在屏幕原位置。
- [x] 用户重新框选得到`L 0.4927 / T 0.0482 / R 0.7095 / B 0.1088`；截图明确显示“模型局部归一化，随模型移动/缩放”，进入v0.4.6固化。
- [x] **部分改善但仍失败**：呆毛不再误绑外部兔耳/尾巴，乱跑程度较前几轮减轻，但极限触屏跟随与动态下仍明显乱跑；v0.4.6改为三项运动贡献可调，不再更换绑定目标。
- [ ] 呆毛静止外观仍为`56/83/-49/-16/+70`；运动沿用模型原生头发物理，只在原生结果之后围绕自身发根施加固定外观变换。
- [ ] v0.4.4恢复的完整26项程序动作按钮、兔耳`135%/100%`、自主待机与ZIP预设均无回归。

### v0.4.4-full-actions-head-rig-test（构建成功、部分失败）

- [x] 程序动作区重新显示优化自主待机前的完整26项按钮，顺序为点头至摸头疑惑彩蛋；该恢复结果进入v0.4.5继续保留。
- [x] **失败**：摸头范围仍按固定屏幕坐标保存/命中，没有移植迷梦随模型边界换算的核心逻辑。
- [x] **失败**：CDI头发候选支撑在极限跟随时仍使呆毛飞到人物左上方；跨Drawable支撑路线废弃。
- [ ] v0.4.3已确认的兔耳`135%/100%`、低频待机双抖、摸头确认范围、持续柔风底座与展示池参数没有被本轮回归。
- [ ] ZIP预设动作/表情保持未修改。

### v0.4.3-idle-correction-test（构建成功、待实机确认）

- [x] 兔耳实机确认值为速度`135%`、幅度`100%`；写成升级预设并加入低频自主待机触发，仍只混回三个兔耳物理输出。
- [ ] 自然眨眼总时长由`0.22秒`放缓为`0.34秒`后，不再显得突然，且不影响动作自带闭眼。
- [ ] 自主待机打开后，原版柔风路线持续循环，随机动作之间及动作运行期间都不出现原地站立。
- [ ] 呆毛加载诊断显示已排除兔耳驱动网格；手动兔耳双抖、点头、摇头时，呆毛不再跟着兔耳摆动且仍能贴合头发整体。
- [ ] 未加入自主待机的13个程序动作按钮已经恢复；加入待机的动作按钮保持隐藏，26条源码路线和ZIP预设均未删除。
- [ ] 开启自主待机至少观察`60秒`可看到展示池开始工作；展示级大摆仅占展示池`14%`，不能把短时间未抽中直接判为未接入。
- [x] 摸头范围截图已确认：`L 0.3875 / T 0.2813 / R 0.6234 / B 0.3778`；固化为版本化默认值，继续保留框选校正和还原确认范围按钮。

### v0.4.2-idle-hair-rig-tuning-test（本轮制作、待实机确认）

- [x] v0.4.1兔耳九轴路线及眼睛隔离已实机确认；v0.4.2只调整其时序/幅度，不回退硬角度输出。
- [x] 兔耳速度、幅度两个滑杆各自独立生效；用户确认最终值为速度`135%`、幅度`100%`，进入v0.4.3版本化预设。
- [ ] 呆毛覆盖安装后固定为`56/83/-49/-16/+70`，但v0.4.2支撑点错误选到兔耳；由v0.4.3改为主动探测并排除兔耳驱动Drawable。
- [ ] 尾巴始终固定在右侧；界面不再允许误切回左侧。
- [x] 摸头框选两点后正确显示`L/T/R/B`四项坐标；用户已回传并用截图确认最终范围，进入v0.4.3固化。
- [ ] v0.4.2仍可看到完全静止，柔风替代方式不符合需求；v0.4.3改为原版柔风双周期连续底座。
- [ ] v0.4.2误删全部26个程序动作按钮；v0.4.3只隐藏待机职责路线并恢复其余13个入口。
- [ ] ZIP预设动作/表情没有被本轮修改。

### v0.4.0-interaction-transition-test（实机需调整）

- [x] 极限触屏跟随、摸头与疑惑大小眼入口已经制作并可触发。
- [ ] 兔耳路线错误地回到直接角度输出；v0.4.1恢复九轴物理。
- [ ] 程序动作仍存在中间停顿和部分结束瞬移；v0.4.1更换连续插值与动作交叉过渡。
- [ ] 点选发根会改变呆毛朝向且无数值显示；v0.4.1保留五项值并增加完整参数显示。

### v0.3.9-motion-parity-test（本轮制作、待实机确认）

- [ ] 呆毛首次安装和覆盖升级后默认为`56/83/-49/-16/+70`，发根仍固定，滑杆仍可继续调整。
- [ ] 耳朵由两次快速九轴脉冲自然上抖，不出现直接改耳角造成的僵硬，也不会变成全身大幅点头。
- [ ] 24个动作的节奏、往返路线和幅度接近迷梦已确认版本，尤其是点头、摇头、风摆和视频式环绕。
- [ ] 动作期间头身、头发、耳朵和尾巴保持明显运动；左右整条手臂不再出现过大的甩动或九轴拉扯。

### v0.3.8-outfit-presets-test（实机已确认）

- [x] 三套服装已做成独立代码预设，不提交用户完整采集JSON、`.vtube.json`或购买模型；切换服装不需要反复导入文件。
- [x] 女仆装、白衬衫、兔女郎三个按钮均能得到目标部件组合，且兔女郎切回其他服装时不会残留灰色Drawable配色。
- [x] 白衬衫关闭`Maid Headband / Hair_behindEar10`，三套服装的`ArtMesh210`与`ArtMesh276`均显示用户指定的`#444573`。
- [x] 服装切换没有把九轴、眼球、手臂、呼吸、表情或物理冻结在采集瞬间。
- [x] v0.3.8耳朵双抖已确认能动但仅“勉强可以”；不作为最终动作基线，v0.3.9改走慢眨眼的九轴→物理链路。
- [x] 用户已通过滑杆确认呆毛最终五项数值，v0.3.9负责固化。
- [x] 迷梦动作幅度复刻与Sen手臂统一降幅按计划留到v0.3.9，未污染服装预设验收。

### v0.3.7-dynamic-foundation（已制作，待实机确认）

- [x] 远程源码 commit：`612c9cabd9278c3962b67f5f009d159f76e6d733`；Actions run `33306331502` 为 `success`，确认Java编译、APK打包、测试签名和产物上传成功。
- [x] APK：`3,768,259` bytes，SHA-256 `ba7a735de8cdefc086290f71c338b9cb3d14166922e1d541ac95082170ebe226`；Actions ZIP：`3,252,470` bytes，SHA-256 `8c5d8ff9aedce0bcffd5226f25c6e1d11d729cddf511e309772ed3181d141332`。
- [ ] 删除耳鳍角度与上下位置的人工网格变形；耳鳍恢复 VTS 采集底值，后续只通过模型原生参数/物理运动。
- [ ] 呆毛保留大小、旋转、左右、上下四轴，并新增独立“宽度”轴；五项变换都以发根为固定连接点，当前数值继续作为试调值而非最终验收值。
- [ ] VTS 581项采集值由“完全冻结画面”改为“每帧恢复的外观底座”；动态叠加顺序固定为：VTS外观底座 → 情绪/动作 → 原生物理 → 尾巴镜像与呆毛几何。
- [ ] 测试面板提供20个AI伴侣情绪入口（紧张、慌张可共享同一视觉，但保留两个语义标签）、Sen ZIP原生表情按钮和24个迷梦移植动作按钮。
- [ ] 原生物理重新启用；尾巴通过呼吸输入驱动模型自带8节物理链自然摆动，不手工逐节改网格。
- [ ] 新增“耳鳍快速上抖两次”测试动作：以短脉冲驱动耳鳍物理输入，不再移动耳鳍Drawable。
- [ ] 自主待机默认关闭，可在测试面板打开；打开后从常规动作池低频随机抽取，并避免连续重复。展示级大动作只保留手动测试，不进入普通随机池。
- [ ] 摸头和彩蛋本轮不实现；512/1024蒙版流畅度等动态动作实机可运行后再比较。

### v0.3.3-mask-ab-test（本轮制作、待实机确认）

- [x] A“默认单缓冲”旧问题由诊断解释：60组超过单缓冲36组上限，是v0.3.2长期异常的同一基线。
- [x] B“动态多缓冲”实测D2/O0后全部部件恢复，但明显锯齿会切割嘴等细节，判定为诊断成功、正式方案不可用。
- [x] C“高精度蒙版”实测全部部件正常；仅耳朵边缘残留细锯齿/白边，进入蒙版分辨率优化。
- [x] 切换A/B/C只改变官方Cubism蒙版策略，VTS冻结参数、79项颜色和模型文件相同。
- [ ] 调整模型时连续双指放大、缩小和松开后人物不会消失；缩放焦点保持稳定，点击还原仍能可靠复位。

### v0.3.2-vts-frozen-parity（实机已确认失败）

- [x] 同时选择 `Sen.vts-profile.json` 与当前 `.vtube.json` 后，界面显示“VTS冻结参数581项”和ArtMesh颜色79项。
- [x] 冻结模式不启动任何表达式、物理或每帧参数更新。
- [x] `Warning2=-1`等越界值已通过Core直写保留，但右上白块完全不变；越界参数不是该批显示异常的主因。
- [x] 五项静态异常均未改善，并新增确认耳朵错层/变厚及耳下深紫色小块。
- [x] “调整模型”缩放存在模型偶发消失；点击还原可恢复，证明是舞台矩阵/手势问题。
- [ ] 下一版先统计实际蒙版组数、Offscreen数量、Blend Mode/高精度蒙版状态，再根据结果动态增加蒙版缓冲或强制高精度蒙版；不得先隐藏错误Drawable。

### v0.3.1-vts-layered-appearance（实机已确认失败）

- [x] 能导入 `xiaojingyu.exp3.json` 与当前 `.vtube.json`，并匹配79项逐ArtMesh颜色。
- [x] 实机颜色与大部分衣服/尾巴选择出现，但静态几何和透明度仍明显错误，不能验收。
- [x] `VTS_Add`转标准`Add`已被新证据否定；`Warning2`目标值超出声明范围，Framework标准写入会裁剪。
- [x] 单一ExpressionManager会让ZIP表情替换基础外观；这是后续表情架构问题，但不先于静态显示修复。

### v0.3.0-vts-profile-static（废弃路线，实机已确认失败）

- [x] 代码和APK曾完成：导入581项API最终快照、应用378项静态值、排除203项动态值。
- [x] 实机结果严重错误：头发、尾巴、裙子、腰带和鞋子大面积退回白模，发型与VTS不同，头顶仍有异常白块；只有部分部件选择和眼睛/皮肤接近。
- [x] 根因已定位：API快照混合了原始默认值、表达式增量和VTS计算后的结果；把最终结果再次设成模型底值会重复或错误计算。逐ArtMesh颜色又不在普通参数快照中。
- [x] 禁止把581项最终快照作为生产状态每帧写入；它会锁死物理、跟随和动作。v0.3.2允许一次性“全部直写后冻结”，仅用于把状态问题与渲染器问题分开，不得误当最终运行架构。
- [ ] 鞋子细化暂不阻塞本轮：用户确认 `Shoes=1.49` 的单侧外观约由鞋体、已染色鞋带和未染色绑腿三个绘制部件组成，后续优先按未染色区域识别绑腿。
- [ ] CDI进一步确认模型存在独立部件“鞋子”(Part103)、“皮鞋”(Part104)、“高跟鞋”(Part106)、“绑带”(Part80)和“腿环”(Part108)；首个静态复现包不强行隐藏，待画面正常后逐个切换确认未染色绑腿对应项。

### v0.2.1-native-texture-fix（本轮）

- [ ] 26张2K贴图不生成 mipmap，官方 Java Framework 的每帧过滤同步改为 `GL_LINEAR`，人物不再是纯黑剪影。
- [ ] 贴图颜色、透明区域和剪贴蒙版能正常显示。
- [ ] 状态栏明确显示 `GL_LINEAR`，可与旧 v0.2.0 黑影包区分。
- [ ] 修复贴图以后，记录所有仍然异常的头发、眼睛、外观和部件；这些属于下一关的 VTS 初始参数复现，不与黑影修复混为一谈。

### Sen VTS Parameter Capture v0.1.0（电脑端，本轮）

- [x] Windows EXE 能连接电脑端 VTube Studio 的官方 WebSocket API；首次连接能正常弹出授权。
- [x] VTS 载入 Sen 后，能读取当前模型、Live2D 参数、输入参数、活动表情、热键和物理设置。
- [ ] “正常待机”和手动开启的表情/动作能分别命名采集；每次连续读取9帧并取中位数。
- [x] 能保存 `Sen.vts-profile.json`，且文件不包含贴图、moc3或授权令牌。
- [x] 当前版本只负责采集，不直接修改 VTS；Android v0.3.0 已开始导入它输出的参数包。

### v0.2.0-native-renderer-test

- [x] 使用官方 `CubismJavaFramework` 5-r.5 与 `Live2DCubismCore.aar`，不经过 WebView/JavaScript/WebAssembly，能在实机完成139.2 MiB moc3的 Core Model 创建。
- [x] 原始26张2K贴图逐张解码上传，不生成 mipmap；加载进度能明确显示到第几张贴图。
- [ ] Sen 静态画面与 VTube Studio 对照一致：无白色头发大块、眼球/眼眶/眼皮不错误重叠、剪贴蒙版与绘制顺序正确。
- [ ] 自动应用 `.vtube.json` 的 `Watermark` 启动表情后水印消失；“查看原始状态”仍可对照水印原始状态。
- [x] 上方约2/3为原生模型预览，下方约1/3保留 ZIP 表情测试按钮。
- [x] App 无需联网或单独导入 Web Core；APK 内的 Java Core AAR可在用户 arm64-v8a 设备正常加载。
- [x] v0.2.0 固定使用仓库内公开 debug keystore；从本版开始后续测试包可直接覆盖安装。v0.1.x 临时签名与本版不同时，首次迁移需卸载旧版并重新导入 ZIP。

### v0.1.4-black-screen-fallback

- [ ] 监听 `webglcontextlost`，不再让 WebView 在显存上下文丢失后静默停在黑屏。
- [ ] 模型完成后对首帧进行网格采样，显示画布尺寸、模型画布尺寸以及可见采样点；可区分“已绘制但位置不对”和“整张画布确实全黑”。
- [ ] 原始2K发生上下文丢失或首帧全黑时，自动生成并切换1K兼容贴图；原始2K模型、贴图和购买文件保持不变。
- [ ] 测试面板可手动在“原始2K / 1K兼容”之间切换，方便确认手机与 WebView 的实际承载上限。

### v0.1.3-safe-touch

- [ ] 修复用户松开手指时，精简舞台未创建官方示例齿轮对象却调用 `null.isHit()` 的错误。
- [ ] 齿轮等示例 UI 被删除后仍可正常触摸模型，点击模型只进入模型自身的点击逻辑。
- [ ] 错误标题由容易误判的“渲染失败”改为“舞台运行失败”；触摸、加载、释放阶段的 JavaScript 错误不再一律归因于渲染器。

### v0.1.2-safe-reload

- [ ] 修复页面重载时背景/齿轮占位资源尚未创建却调用 `null.release()` 的错误。
- [ ] View、Subdelegate、Delegate 的资源释放流程允许空值并可重复调用。
- [ ] 旧页面退出阶段的异常不再覆盖新页面的真实加载进度。

### v0.1.1-renderer-diagnostics

- [ ] 修复26张2K贴图同时解码造成的内存峰值：保持2K原图，改为逐张解码和上传。
- [ ] 贴图上传后释放 CPU 端解码图片，不生成移动端当前视图不需要的 mipmap。
- [ ] 状态栏能分别显示 model3、约139 MiB moc3、模型创建、表情/物理以及贴图 `n/26`。
- [ ] 任一贴图解码或 WebGL 上传失败时，显示具体编号和错误，不再永久停在“正在加载”。

### v0.1.0-renderer-test

- [ ] 官方 Cubism 5 Framework 能正确加载 Sen 的 Cubism 5 `.moc3`。
- [ ] 头发不再出现白色大块或错误叠层。
- [ ] 眼球、眼眶、眼皮的蒙版和绘制顺序与 VTube Studio 一致。
- [ ] 自动读取并应用 `SavedActiveExpressions`，Watermark 启动后消失。
- [ ] “查看原始状态”会重新载入模型但不应用 VTS 启动表情，便于对照。
- [ ] 下方 1/3 面板能列出 ZIP 中补登记的表情并单独测试。
- [ ] 本地 Core 导入和官方联网 Core 两种方式都能启动。

## 实机已确认

- v0.2.0 原生版能完成139.2 MiB moc3创建和26张2K贴图上传，能绘制完整人物几何轮廓；因此原生 Core、模型矩阵和主要 Drawable 绘制链路已经运行。
- v0.2.0 最终人物为纯黑剪影。直接代码对照确认：App只上传贴图 level 0、不生成 mipmap，但官方 `CubismShaderAndroid.setUpTexture()` 每帧又将 `GL_TEXTURE_MIN_FILTER` 强制设为 `GL_LINEAR_MIPMAP_LINEAR`。没有完整 mipmap 的贴图会被 OpenGL 当作不完整贴图并采样为黑色。该原因已进入 v0.2.1 修复，待实机确认。
- 黑影轮廓仍能看到头顶异常大块，因此头发问题并未因改用原生渲染自动消失；眼球、眼眶、眼皮和其他可选部件也必须视为“可能同时异常”，不能只修头发外观。
- v0.1.4 导入 Sen 2K ZIP 后，上半区保持黑色，进度一直停在“moc3 已读取，正在创建 Cubism 5 模型…”。这说明程序尚未到达表情、物理、贴图解码、贴图上传或首帧采样阶段；1K兼容贴图不会触发，也不能解决当前故障。
- 此前的 `null.release()` 与 `null.isHit()` 均已不再出现，但这只代表官方示例空对象防护生效，不代表模型已经渲染成功。

## 踩坑点（后续 AI 必读）

### VTS最终快照不能作为生产底值，但可用于一次性冻结对照

- v0.3.0实机已经否定“采集全部最终参数→过滤动态项→把其余值设为基线”的方案。API读到的是VTS计算后的当前结果，不是用户实际修改清单。
- 用户实际创建的 `xiaojingyu.exp3.json` 只有55项，且基本全为 `Add`。例如原模型 `ParamHair_Hue2=4.1`，表情增加约`-0.4498`后才得到采集值约`3.6502`；若把`3.6502`直接设为底值再叠加表情，结果必然错误。
- 用户当前 `.vtube.json` 的 `ArtMeshDetails.ArtMeshMultiplyAndScreenColors` 还有79项逐Drawable颜色。它们不属于Live2D参数API快照，却正是紫色头发、服装、尾巴、鞋子等配色的重要来源。
- v0.3.1实机已经否定“原模型默认值→实际exp3增量→VTS颜色”足以完整复现Sen。该顺序仍可解释配色和部分换装，但不能解释VTS专用越界参数、输入映射中性值和运行时状态。
- 当前最关键证据：`Warning2`模型范围为0到1，默认1；VTS采集当前值却为-1。`xiaojingyu`中该参数使用 `VTS_Add`，不能改成标准`Add`后交给会裁剪范围的Framework API。
- v0.3.2仅为诊断而把581项值一次性直写Core并冻结。如果结果正确，后续再从完整快照中拆出可持久化基础层；如果仍错误，才说明状态值不足以解释差异。
- 官方 Cubism Java 能按Drawable ID设置运行时乘算色和屏幕色；不要为了复现VTS颜色修改购买模型PNG，也不要把581项最终快照做成永久每帧覆盖。

### 长期静态异常必须先于动作和表情解决

- 用户用旧迷梦APK放大确认原始模型状态存在：头顶右侧巨大白块、嘴只剩小点、左右手旁各有浮空白线（更可能是缺失/错位的手指）、刘海选择缺失、白色丝袜接近透明。早期脸周半透明暗红圈在新原生版已不再出现。
- 同一批异常从首次Sen测试APK持续到v0.3.1，不是VTS采集器、JSON导入或后续表情按钮新造成的。表情替换基础外观是另一个已确认问题，但必须排在静态显示之后。
- 丝袜不是单一网格开关：模型同时有 `SockR`、`SockR2`款式参数及 `SockR_Opacity`、`SockL_Opacity`透明度参数。刘海也同时存在发型选择和 `BangX/BangY`物理输出。不得继续按单部件开关猜测。
- 错误图层/Drawable点击定位只作为底层静态对照失败后的后备方案，不能用隐藏坏网格掩盖初始化和变形语义错误。

### 小数参数没有被取整；优先检查蒙版容量与渲染模式

- `SenVtsProfile` 使用 `optDouble()` 读取后转为Java `float`，再由 `CubismParameterView.setValue(float)` 直接写入Core；没有 `int` 转换、`Math.round/floor/ceil` 或离散开关化。`1.49`、`1.1`、`2.1`足以被float精确区分。
- 实际参数包中的 `SockR=0.754029989...`、`SockR_Opacity=0.200000002...`、`Sidehair9=0.077720612...` 均被保留。项目唯一的 `Math.round` 用于Android界面dp转像素，与模型参数无关。
- 当前 `setupRenderer(renderer)` 使用默认1张剪贴蒙版缓冲。官方Java Framework定义默认单缓冲正常布局上限为36组蒙版；Sen有1742个Drawable，必须在实机统计其唯一蒙版组数，不能假设1张足够。
- 蒙版缓冲复用或高精度蒙版模式不一致，可以统一解释原本互不相邻的白块、缺刘海、耳朵变厚/错层、嘴部只剩小点、手指缺失与丝袜透明度异常。该方向优先级高于逐Drawable隐藏，但仍需下一版输出实际蒙版组数和Blend/Offscreen状态后确认。

### 不生成 mipmap 时必须同步修改官方 Java Shader 的过滤方式

- App 的 `NativeTextureManager` 将26张2K贴图逐张上传，只定义 level 0，并设置 `GL_LINEAR`。但官方 Java Framework 5 R5 会在 `CubismShaderAndroid.setUpTexture()` 的每次绘制中重新设置 `GL_LINEAR_MIPMAP_LINEAR`，覆盖加载阶段的设置。
- “加载全部成功、几何轮廓正确、整个人物纯黑”正是贴图不完整的典型结果，不应再怀疑 moc3、降低分辨率或拆解 VTube Studio APK。
- Sen 使用26张2K贴图，生成完整 mipmap 约增加三分之一贴图显存。当前项目通过仓库补丁 `patches/cubism-java-no-mipmap.patch` 把过滤改为 `GL_LINEAR` 与 `GL_CLAMP_TO_EDGE`，GitHub Actions 必须在 Gradle 构建前应用该补丁。
- `Framework` 是官方 submodule，不能把未推送的本地 submodule commit 当作修复提交；否则 GitHub Actions 无法检出。补丁必须保存在主仓库并由工作流显式应用。

### VTube Studio 是校准基准，不是需要反编译的运行依赖

- Sen 包内 `.vtube.json` 已含62组 `ParameterSettings`、51个热键、物理/平滑设置、模型位置和 `SavedActiveExpressions`。目前 Android 只应用了 Watermark 表情，没有执行完整输入→输出映射，因此原始默认状态不等于 VTS 正常待机状态。
- 不反编译、不复制、不重新分发 VTube Studio。优先使用其官方 Public API 读取当前全部 Live2D 参数、表情、热键和物理设置。
- 电脑采集器位于 `tools/vts-profile-capture/`。第一版输出 `Sen.vts-profile.json`；后续 Android 只导入参数包，不依赖 VTube Studio 持续运行。

### 官方示例对象并不一定存在

- 本项目只移植官方 Cubism 示例的模型渲染部分，背景图、齿轮按钮等示例 UI 会被省略。因此，官方源码里对 `_back`、`_gear` 等对象的直接调用不能原样保留。
- 已遇到两种同源崩溃：页面重载/退出时的 `null.release()`，以及触摸结束时的 `null.isHit()`。前者属于资源释放，后者属于触摸交互，都不是 Sen 模型文件或 Cubism 渲染失败。
- 以后修改固定版本的官方 Sample 后，必须搜索被省略对象的全部成员调用，而不只修复当前堆栈中的一行：`rg "_back\\.|_gear\\." lappview.ts`。初始化、渲染、触摸和释放四条路径都要允许对象为空。
- 页面卸载阶段可能与新页面加载重叠。卸载错误不得覆盖新页面状态；释放函数必须可重复调用，且每个资源在调用 `release()` 前都要判空。
- Android 侧统一错误标题使用“舞台运行失败”。判断原因时以最后一条加载进度和具体属性名为准，不要看到统一标题就擅自降低贴图或怀疑 `.moc3`。

### 无报错黑屏不等于模型损坏

- 官方 `LAppSubdelegate.update()` 发现 `gl.isContextLost()` 后会直接 `return`，默认既不通知 Android，也不抛 JavaScript 错误。因此，“上半区纯黑、没有报错”必须优先检查 WebGL 上下文，而不能继续修 `release/isHit` 或怀疑 ZIP。
- PNG 的磁盘体积不能代表显存体积。Sen 的26张2K透明贴图即使压缩包很小，RGBA 上传后仍约占416 MiB；Cubism 的剪贴蒙版、帧缓冲和模型资源会继续增加峰值。
- 1K兼容模式只在 App 私有目录生成替代贴图和一个替代 `model3.json`，复用原始 `.moc3`、物理、表情和动作；不得覆盖或修改用户购买的原始2K贴图。
- 若1K首帧采样仍全黑，则下一步检查模型矩阵、绘制参数和默认部件状态；不要继续无条件降到更低分辨率。

### 停在“正在创建 Cubism 5 模型”时还没有读取贴图

- 该状态位于 `this.loadModel(arrayBuffer, ...)` 内部，主要包括把 `.moc3` 复制到 Cubism Core 内存、激活 MOC、再创建 Core Model。它是同步调用；WebView 主线程卡在这里时，后续进度、超时回调和1K自动回退都无法执行。
- Sen 的 `.moc3` 为 `145,951,936` bytes（约139.2 MiB）。使用同一官方 Cubism Core 5.1.0 在桌面进程中拆分测量：MOC 激活后进程约488 MiB，Core Model 创建后约503 MiB，尚未加载任何贴图。
- 模型规模为581个参数、229个部件、1742个 Drawable，模型画布为4234×8344。开启官方 MOC consistency 检查还会额外复制整份 `.moc3`；它不是最终内存占用的根因，但 Web 版必须关闭这次不必要的复制。
- Web Cubism Core 使用 WebAssembly 线性内存，扩容与大块复制会放大 Android WebView 渲染进程的峰值。即使随后切到1K贴图，也不能消除创建模型之前已经出现的约500 MiB压力。
- VTube Studio 能正常显示不能反证 WebView 方案可行；VTube Studio 使用原生渲染，不经过 JavaScript/WebAssembly/WebView 这套内存路径。
- 后续不要从 VTube Studio APK 拆取 Core：那既不是可维护的 SDK 来源，也可能违反其软件许可。应使用 Live2D 官方 Cubism SDK for Java 压缩包中的 Core；官方 RedistributableFiles 清单允许在许可条款下把对应运行库随 APK 分发。

### 测试 APK 必须固定签名

- GitHub 托管运行器是临时环境，不能依赖它自动生成的默认 debug keystore；否则不同构建的 APK 可能无法覆盖安装，卸载又会清除 App 私有目录里的大模型。
- v0.2.0 起使用仓库内 `app/debug.keystore`，它只用于公开测试，不是正式发布密钥。不得把它误用于商店正式版或任何需要保密身份的签名。

## 后续任务队列

1. 安装已通过Actions构建的v0.4.8；实机采集发根/方向点并导出联合诊断JSON。根据根点标记、固定效果和导出值固化Sen专用锚点及最终四项形状参数。
2. 用VTube Studio手动逐项确认`ParamMouthX`、`ParamTongueOut`、`ParamMouthFunnel`、`ParamMouthShrug`、`MouthPuckerWiden`、`MouthPressLip`、`ParamJawOpen`和现有表情文件的实际画面；再重做辨识度不足的20个程序情绪组合，禁止只按参数名猜视觉。
3. 按实机反馈继续微调自然眨眼、摸头疑惑平滑度和自主待机动作池；完整26个程序动作按钮在用户逐项确认前继续保留。
4. 呆毛、表情和待机验收后，再规划接入AI伴侣主项目的桌宠模块；DeepSeek对话、情绪标签、TTS口型和Agent功能不在当前独立渲染测试阶段提前混入。

## 新对话/新 AI 接手清单

1. 先完整阅读本总账，再读 `README.md` 和最近提交；不要只看聊天记录。
2. 检查用户最新实机反馈属于“渲染关卡”还是“状态关卡”，禁止混修。
3. 动手前把本轮计划登记到“待实机确认”；完成构建后记录版本、commit和Actions run。
4. 用户说“效果不错/没有问题”后才把对应条目改成“实机已确认”；部分成功必须拆开记录。
5. 不提交用户购买的 Sen ZIP、moc3、贴图、表情或 VTS 配置；电脑采集出的用户参数包也不提交公开仓库。
6. 任何新错误都加入对应踩坑点，尤其记录最后可见进度、版本号和是否已进入贴图绘制阶段。
